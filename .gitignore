.DS_Store
Thumbs.db
node_modules/
.playwright-mcp/

# Client video masters land in the repo root. They are hundreds of MB — over
# GitHub's 100 MB push limit and far over Cloudflare Pages' 25 MiB per-file
# cap — so only the encoded copy under docs/assets/ is ever committed.
# Root-anchored, so docs/assets/*.mp4 stays tracked.
/*.mp4
